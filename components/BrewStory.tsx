"use client";
import { motion, useScroll, useSpring, useTransform } from "framer-motion";
import { useRef } from "react";
export default function BrewStory(){const ref=useRef<HTMLElement>(null);const {scrollYProgress}=useScroll({target:ref,offset:["start start","end end"]});const p=useSpring(scrollYProgress,{stiffness:70,damping:24});const fill=useTransform(p,[.08,.88],["8%","92%"]);const water=useTransform(p,[0,.3],[0,1]);const coffee=useTransform(p,[.28,.68],[0,1]);const steam=useTransform(p,[.65,1],[0,1]);const deg=useTransform(p,[0,1],[-30,210]);return <section ref={ref} id="receta" className="brew-wrap"><div className="brew-sticky shell"><div className="brew-copy"><p className="eyebrow">SCROLL 02 · LA RECETA</p><h2>Una taza equilibrada también es una cuestión de proporción.</h2><div className="brew-metrics"><div><b>18 g</b><span>café</span></div><div><b>36 g</b><span>bebida</span></div><div><b>28 s</b><span>tiempo</span></div></div></div><div className="brew-stage"><div className="brew-ring"><motion.i style={{rotate:deg}}/></div><div className="brew-cup"><motion.div className="brew-fill" style={{height:fill}}/><motion.div className="brew-water" style={{opacity:water}}/><motion.div className="brew-coffee" style={{opacity:coffee}}/><motion.div className="brew-steam" style={{opacity:steam}}><i/><i/><i/></motion.div></div><span className="ratio">1 : 2</span></div><div className="brew-foot"><span>AJUSTAR</span><span>EXTRAER</span><span>PROBAR</span><span>REPETIR</span></div></div></section>}
